# Force npm to use legacy peer deps resolution (same behavior as bun install)
# This fixes the deploy build on platforms that use `npm install` instead of `bun install`
legacy-peer-deps=true

# Also speed up install by reducing audit/audit issues
audit=false
fund=false
